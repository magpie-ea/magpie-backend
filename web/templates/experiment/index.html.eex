<h2>Manage Experiments</h2>

<%= link "New experiment", to: experiment_path(@conn, :new), class: "btn btn-primary" %>

<table class="table">
  <thead>
    <tr>
      <th>Experiment ID</th>
      <th>Author</th>
      <th>Description</th>
      <th>Submissions</th>
      <th>Active?</th>

      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= for experiment <- @experiments do %>

      <tr>
        <td><%= experiment.experiment_id %></td>
        <td><%= experiment.author %></td>
        <td><%= experiment.description %></td>
        <td>
          <%= experiment.current_submissions %>
          <%= if experiment.maximum_submissions do %>
            /
            <%= experiment.maximum_submissions %>
          <% end %>
        </td>
        <td><%= experiment.active %></td>

        <td class="text-right">
          <%= link "Toggle activation status", to: experiment_path(@conn, :toggle, experiment), class: "btn btn-warning btn-sm" %>
        </td>
        <td class="text-right">
          <%= link "Retrieve experiment results", to: experiment_path(@conn, :retrieve, experiment), class: "btn btn-success btn-sm" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
